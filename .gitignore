node_modules/
dist/
.wrangler/
.env
.env.*
.dev.vars
.dev.vars.*
!.dev.vars.example
*.log
.DS_Store
coverage/
.claude/settings.local.json
# Any shared local D1 passed to `wrangler --persist-to`, whatever it is called. A glob rather than
# a list: `.wrangler-local/` was named here and `.wrangler-verify/` still reached a commit, because
# the next one always has a different name.
.wrangler-*/
.pnpm-store/

# VAPID keypair written by workers/cron/scripts/generate-vapid.mjs. Never commit a private key.
.vapid.json
