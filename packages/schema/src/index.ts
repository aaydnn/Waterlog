// Shared payload shapes for WaterLog. Zod schemas mirroring the D1 tables
// land here in T0.2; both the web client and the workers import from this
// package — payload shapes are never defined anywhere else.
export const SCHEMA_VERSION = 1
